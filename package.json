{
  "name": "playscout",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:data": "node scripts/sync-sources.mjs",
    "build": "node scripts/build.mjs",
    "validate": "node scripts/validate.mjs",
    "check": "npm run build && npm run validate",
    "serve": "python3 -m http.server 4174 -d dist"
  },
  "engines": {
    "node": ">=18"
  }
}
